# Stack
- GitHub Actions reusable YAML workflows on Ubuntu runners; conditional .NET or Node setup for application callers.
- Docker Buildx builds and publishes application images to GHCR.
- SemVer Git tags identify versioned images; OCI revision labels verify the source commit on reruns.
- Python regression tests cover latest-tag promotion without registry access.
- Tool versions and permissions belong to the workflow definitions.
